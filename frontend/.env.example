# ── Vite uses VITE_ prefix for env vars exposed to the browser ────────────────
# The Vite dev server proxies /api to the backend (configured in vite.config.js)
# so no API URL needed here for development.

# Only needed if you deploy frontend separately from backend:
# VITE_API_BASE_URL=https://your-backend-domain.com/api
